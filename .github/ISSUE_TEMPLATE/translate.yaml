# This file is auto-generated!
name: Translation
description: Provide a translation for in-game strings.
title: 'Translation: '
labels:
  - translation
assignees:
  - p3lim-wow
body:
  - type: dropdown
    id: locale
    attributes:
      label: Which locale are these translations for?
      options:
        - deDE (German)
        - esES (Spanish, Spain)
        - esMX (Spanish, Mexico)
        - frFR (French)
        - itIT (Italian)
        - koKR (Korean)
        - ptBR (Portuguese, Brazil)
        - ruRU (Russian)
        - zhCN (Chinese, Simplified, PRC)
        - zhTW (Chinese, Traditional, Taiwan)
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        Please translate each sentence below into the fields next to them.  

        ---
  - type: input
    attributes:
      label: '"Artwork opacity"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Artwork style"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Button scale"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Hide cooldown text"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Only show for quests in current zone"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Only show for tracked quests"'
      placeholder: Translation here
    validations:
      required: true
  - type: input
    attributes:
      label: '"Tracking distance"'
      placeholder: Translation here
    validations:
      required: true
